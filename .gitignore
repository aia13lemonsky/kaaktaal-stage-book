# Real song content stays out of the (public) repo — shared with the band
# directly and imported via the app's own Import button, not baked into source.
kaaktaal-song-format-guide.md
kaaktaal-songs.json
